# territory-report-{{id}}.md — 叶子 agent 产出（只读测绘）

> 由**只读叶子 agent** 写。**只写本文件**，不碰 survey-state/log。
> **不出现密钥/PII 原文**（只记键名/位置/用途）。

## Territory 信息
- id: `{{id}}`
- 边界: `{{glob}}`
- 已读文件: `{{清单}}`
- 未读/超预算文件: `{{清单}}` → 入未解之谜

## 维度发现（按 dimensions.md，本 territory 涉及的）
### {{维度}}
- 关键文件: `path :: 符号`
- 结论: {{...}} `confidence: 已确认|推断|未解之谜`
  - 已确认 → 必须是字面存在性、可 grep（写出回查用的「文件+符号」）
  - 行为/关系/顺序/运行期 → 最高「推断」

## 跨边线索（供主 agent SYNTHESIZE 归并）
- 本 territory 引用了外部符号: `外部符号` ← 出现于 `本territory文件 :: 符号`（跨边，纯模式标推断）

## 本 territory 未解之谜
- {{未读文件 / 需运行确认 / 存疑}}

## 敏感项（只记键名/位置/用途，禁原文）
- `{{KEY_NAME}}`（位置: `{{path}}`，用途: `{{...}}`）
