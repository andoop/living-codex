# 旅程 · {{旅程名}}

> **Confidence 图例（逐节必读）**：`已确认` = 仅字面存在性、已 grep 核验，非语义/运行验证；`推断` = 未运行验证的判断（**所有流程/连边/因果结论上限**）；`未解之谜` = 需运行或未读到。
> 〔**本文件为推断级重建**：以下流程/规则均基于静态阅读，未运行验证。行内只标**与此基线不同**的例外。〕

---

## L2 · 端到端流程（主干）
{{用户做 X → 系统做 Y → 得到 Z。只给主干顺序，分支收进 L3 折叠。}}

- 入口：{{入口符号}} 〔已确认(字面)：grep 命中 "{{符号}}" @ {{文件}}〕
- 步骤：{{步骤序列，行为性结论默认落在上方推断基线内}}

<details>
<summary><b>L3 · 场景与规则（展开看分支/条件/边界/异常）— 细节主体在此</b></summary>

### 规则 / 分支（决策级具体度，禁"含糊+推断"双免责）
- {{规则}}：{{具体条件/阈值/分支/产物}} 〔推断；锚点 {{文件}} :: {{符号}}〕
- {{需运行才能定的}} → 〔未解之谜：为何没查清 = {{原因}}；该读 {{文件}}〕

> dispatcher 分支（B2）：若本入口分发多业务（`when(action)`/路由表），逐 action 展开为独立节点，不以"1 入口已枚举"掩盖 N 子行为。

</details>

<details>
<summary><b>L4 · 机制实现（状态机 / 数据模型 / 时序）</b></summary>

```mermaid
%% TODO: 状态机 / 时序图占位
stateDiagram-v2
    [*] --> {{state}}
```
> *本图为静态推断重建，非验证协议。图中关系未经运行验证，不代表确定的执行顺序/因果。*

</details>

## L5 · 代码锚点
- [[portraits/{{module}}#{{符号}}]] — {{若长尾无 portrait：直链 `文件:符号` 并标「证据层未建画像」}}

## 跨流程连边（节点 + 边）
- 本旅程经异步边界劈成多段：见 [[edge-ledger]] 中边 {{edge-id}}（连接节点 {{N_i}}↔{{N_j}}）。
- 断裂边（对端未找到 = 已知静态盲区）：{{若有}}

## 多角色追踪记录
- 正常流程 / 数据·状态 / 失败·边界 三角色结论；**分歧处**：{{分歧描述 → 调和结论 or 降未解之谜标分歧}}
