# 变更史 · {{产品名}} 业务导览书（log）

> OKF 文档变更史（OKF §7）：**这本书**哪天改了什么。ISO 日期、**倒序**（最新在上）、**append-only**（只追加不改写）。
> **本文件不带 frontmatter。**
> 注意：这里只记**书的变更**，不记结论的取证轨迹——证据轨迹归各文档的 `## 证据与可信度`（见 `references/plain-language.md` §8）。

---

## {{YYYY-MM-DD}}
- {{这次对导览书做了什么改动，白话一句}} — 快照 commit `{{hash}}`

## {{YYYY-MM-DD}}
- 首次生成 L0–L5 业务导览书 — 快照 commit `{{hash}}`
