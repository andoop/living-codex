---
type: rule
title: "{{白话规则名}}"
description: "{{一句白话：这条规则在管什么}}"
tags: ["{{领域标签1}}"]
timestamp: "{{YYYY-MM-DD}}"
---

# 业务规则 · {{规则名}}

> 🟡 **可信度（默认必读）**：下面是**基于读代码的推断**，没实际跑验证。正文只额外标 `已确认（字面存在）` 与 `未解之谜（没查清）` 两种例外。完整三档释义见文末「证据与可信度」。

## 规则陈述（白话且决策级 · 线上）
{{用产品语言写："当 X（具体条件/阈值）则 Y（具体产物/分支）"。白话但**不含糊**——给出条件/阈值/分支/产物，技术锚点下沉证据区。}}

示例（低视觉噪声 · 段基线=推断时普通行为句不重复标）：
> 上传失败时会自己重试，最多 3 次，还不行就把这次任务标成失败、等下次再说。
> 只对例外行内标：上限 3 次〔已确认：代码里确实有 "RETRY_MAX = 3"〕，超限后是否上报埋点〔未解之谜：需运行确认〕。

❌ 禁止：无标记的裸肯定式行为句脱离任何基线段。
❌ 禁止：`会做重试处理`（含糊 + 推断双免责，见 `usefulness-floor.md`）。

## 还不知道什么（gap · 白话 · 线上可见）
- {{没查清/需运行才能定的}}〔未解之谜：原因={{…}}；该读 {{文件}}〕

---

<details>
<summary><b>证据与可信度（线下 · confidence 三档 + 锚点）</b></summary>

> **完整三档释义**：`已确认` = 仅字面存在性、已 grep 核验；`推断` = 未运行验证的判断（行为结论上限）；`未解之谜` = 需运行或未读到。

| 条件 | 行为 | confidence | 锚点（文件 :: 符号） |
|---|---|---|---|
| {{条件}} | {{具体产物}} | 推断 | {{文件}} :: {{符号}} |

### 代码锚点
- [[portraits/{{module}}#{{符号}}]] / 或直链 `{{文件}}:{{符号}}`〔证据层未建画像〕

</details>
