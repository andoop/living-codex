# 99 · 未解之谜（集中暴露所有不确定 / 需运行确认）

> 本页是诚实红线的兑现处：凡需运行、需精读未读区、或本测绘降级未覆盖的，全部集中在此。
> **本书任何"已确认"仅 = 字面存在性；行为/运行期结论一律未在此外被升级。**

## A. 部分测绘 / 未精读区（成本护栏降级）
- `src/lib/ingest.ts` 正文（~118KB / 数千行）仅读导出符号与函数签名，**正文控制流未逐行精读**。
- `src/components/**`（~77 项，含 graph-view.tsx ~62KB、chat-message.tsx ~39KB、settings 各 section）仅清单级扫描，**未逐文件精读**。
- `mcp-server/**`、`extension/**` 仅看入口/manifest，未精读实现。
- 海量 `*.test.ts`（尤其 `embedding.test.ts` ~73KB）未精读。
- `src-tauri/src/commands/fs.rs`（~85KB）、`search.rs`（~47KB）、`vectorstore.rs`（~44KB）、`file_sync.rs`（~60KB）仅读头部与符号，**路由/实现正文未逐行读**。

## B. 需运行才能确定（行为 / 运行期）
- 所有 IPC 命令、HTTP API 端点的**真实调用路径、可达性、鉴权**。
- Ingest 各阶段（分析→生成→合并→写盘→嵌入）的**真实顺序与错误处理**。
- 三路检索（全文 / 向量 / web）的**实际融合权重与触发条件**。
- LLM provider 分派、流式解析差异、取消/重试/超时语义。
- 向量库 v1→v2 迁移、optimize/compaction 的触发时机。
- 去重判定阈值与优先级。

## C. 安全待确认（见 [[95-risks]] / [[personas/security]]）
- 本地 HTTP API（19828）是否鉴权、绑定地址（127.0.0.1 vs 0.0.0.0）。
- LLM/搜索 `apiKey` 是否加密落盘于 `app-state.json`。
- 文件命令路径穿越防护是否完备；子进程 CLI 参数注入面。
- 第三方解析库（pdfium/calamine/docx-rs/office_oxide）攻击面与 `run_guarded` 覆盖范围。

## D. 历史不可测（见 [[90-history]]）
- TARGET 工作副本 git 历史被截断（仅 1 条提交）→ 热点目录 / 演进史无法测绘，需完整仓库历史。

## E. 矛盾 / 存疑（advisory，未擅自消解）
- 暂未发现明显文本内部矛盾。本测绘**未运行任何测试与应用**（铁律：只读 TARGET、不验证运行行为）。

> 提醒：以上任一条目若要落为"已确认"，需具备确定性运行时证据（运行 / 解析器后端），本纯模式测绘明确不假装具备。
